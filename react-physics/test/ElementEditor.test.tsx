/*import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import React, { useState } from 'react';

export interface Animateables {
    id: string;
    className: string;
    x: number;
    y: number;
    type: string;
}

describe('Greeting', () => {
    it('renders a box on screen, just one', () => {
        [elements, setElements]= useState<Animateables[]>([]);

        render(<ElementEditor elements={elements} setElements={setElements} />);

        const box = screen.getByTestId('element-0')
        expect(box).toBeInTheDocument();
    });
});*/